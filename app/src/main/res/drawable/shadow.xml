<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#242B47" /> <!-- Change the color as needed -->
    <corners android:radius="10dp" /> <!-- Optional: Round the corners -->
    <size
        android:width="100dp"
        android:height="50dp" /> <!-- Set the size of the rectangle -->
    <gradient
        android:angle="100"
        android:endColor="@color/navy"
        android:startColor="@android:color/transparent" />
</shape>